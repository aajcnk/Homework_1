<h2><%= params[:my] ? 'Мои' : 'Все' %> петиции</h2>

<table class="table">
  <thead>
  <tr>
    <th>Петиция</th>
    <th>Автор</th>
  </tr>
  </thead>
  <% @petition.each do |petition| %>
    <%= render 'petition', petition: petition %>
  <% end %>
</table>
